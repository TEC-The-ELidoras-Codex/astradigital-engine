# TEC_OFFICE_REPO Environment Variables
# IMPORTANT: Copy this file to .env and fill in your credentials
# DO NOT commit the filled .env file to source control!

### REQUIRED VARIABLES ###

# WordPress Configuration (Required for WordPress posting)
WP_URL=https://your-wordpress-site.com        # Base URL of your WordPress site
WP_USERNAME=your_wordpress_username           # WordPress username
WP_PASSWORD=your_wordpress_app_password       # WordPress application password (NOT your login password)
                                              # Generate at WordPress Admin > Users > Your Profile > Application Passwords
WP_XMLRPC_PATH=/xmlrpc.php                    # Path to xmlrpc.php (usually "/xmlrpc.php")

# AI Provider Configuration (At least one AI provider is required)
OPENAI_API_KEY=your_openai_api_key            # Required for Airth's main functionality
OPENAI_MODEL=gpt-4-turbo                      # OpenAI model to use (gpt-4-turbo recommended)
ANTHROPIC_API_KEY=your_anthropic_api_key      # Optional alternative provider

### OPTIONAL VARIABLES ###

# Hugging Face Configuration (Optional - only for HF Space deployment)
HF_TOKEN=your_huggingface_token
HF_USERNAME=your_huggingface_username
HF_SPACE_NAME=your_space_name

# GitHub Configuration (Optional - only for GitHub integration)
GITHUB_TOKEN=your_github_token
GITHUB_REPO=your_github_repo
GITHUB_USERNAME=your_github_username

# Additional AI Provider Configuration (Optional)
ANTHROPIC_MODEL=claude-3-opus-20240229        # Anthropic model to use

# Logging Configuration
LOG_LEVEL=INFO                                # DEBUG, INFO, WARNING, ERROR, CRITICAL
DEBUG=false                                   # Enable debug mode (true/false)

# Agent-Specific Configuration
AIRTH_PERSONALITY=confident, intelligent, slightly sarcastic
BUDLEE_PERSONALITY=efficient and precise
SASSAFRAS_PERSONALITY=creative and chaotic
